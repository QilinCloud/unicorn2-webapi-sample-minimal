# Samples Legacy Landscape

The historic Unicorn ApiWeb sample exposed many operations at once.

This repository deliberately keeps only a small supported operation set so that a third-party developer can start with a realistic, maintainable connector.

