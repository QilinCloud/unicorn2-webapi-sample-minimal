# Minimal ApiWeb Sample Architecture

The minimal sample uses the same front controller, authentication, request envelope, and response contract as the complete sample. Only the exposed method functions differ.

