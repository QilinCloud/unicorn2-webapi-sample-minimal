# ApiWeb Interface Mirror Components

- `class.*.php`: DTO mirrors for articles, categories, orders, documents, returns, and helper types.
- `enum.*.php`: enum mirrors for Unicorn values such as currencies, payment types, status values, and units.
- `class.ApiWeb*.php`: ApiWeb-specific transport DTOs.

