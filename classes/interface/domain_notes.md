# ApiWeb Interface Mirror Domain Notes

Numeric enum values are protocol-sensitive. Keep them aligned with the C# Unicorn 2 interface enums before publishing sample updates.

DTO fields are intentionally permissive because partner endpoints usually receive decoded JSON objects rather than fully constructed PHP class instances.

