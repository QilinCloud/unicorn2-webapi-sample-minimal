# ApiWeb Interface Mirror Workflows

Use these classes as optional helpers when implementing endpoint functions. The front controller passes decoded JSON objects, so endpoint functions can either use raw objects or copy them into these DTOs.

