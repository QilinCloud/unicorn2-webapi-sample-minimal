# Samples Overview

This folder contains the marketplace-facing implementation variants for this standalone sample repository.

In this repository only `minimal/` is active. It shows the smallest useful OTTO Markets style Web API connector surface for order download, shipment feedback, stock updates, price updates and delivery-time updates.

