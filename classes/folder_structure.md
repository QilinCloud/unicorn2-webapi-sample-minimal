# ApiWeb Sample Classes Folder Structure

- Root PHP files: protocol runtime.
- `interface/`: DTO and enum mirrors that make sample code easier to read.

Do not put partner business rules in the runtime classes.

