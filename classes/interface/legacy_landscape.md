# ApiWeb Interface Mirror Legacy Landscape

This folder descends from the older ApiWeb sample model. The modernized version keeps the old class names but updates ApiWeb-specific DTOs, authentication assumptions, and important enum coverage.

