# Samples Domain Notes

The minimal sample intentionally reports fewer capabilities than the complete sample.

Third-party developers should only return capabilities that their connector can handle reliably in unattended operation.

