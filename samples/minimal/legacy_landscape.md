# Minimal ApiWeb Sample Legacy Landscape

Older ApiWeb examples tended to include many endpoints by default. This minimal sample exists so third-party developers can start with the few required operational flows and expand later.

