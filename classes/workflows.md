# ApiWeb Sample Classes Workflows

`api.php` creates `Request`, authenticates it with `Authentification`, invokes an implementation function, appends `Result` objects to `Answer`, and sends the signed JSON response.

