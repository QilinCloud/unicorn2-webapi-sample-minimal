# ApiWeb Sample Classes Domain Notes

These classes are sample infrastructure, not business logic. Minimal OTTO-specific behavior is implemented in `samples/minimal/sync.php` and `classes/class.otto.php`.

Keep runtime changes compatible with PHP 8.x and avoid extensions beyond `json`, `hash`, and `openssl`.
