# ApiWeb Interface Mirror Architecture

Most classes are flat DTOs that mirror Unicorn object shapes. `WawiObject` and `MappingObject` provide common identifier behavior, while enum files preserve numeric values expected by Unicorn.

