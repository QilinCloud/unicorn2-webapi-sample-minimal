# ApiWeb Sample Classes Legacy Landscape

Several class names preserve the older German Unicorn interface naming. This is deliberate so legacy developers recognize payload fields and can compare them with existing Unicorn objects.

